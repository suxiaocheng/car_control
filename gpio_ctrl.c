#include "gpio_ctrl.h"

int set_gpio(enum control_pin pin, enum control_status status)
{
	int ret = 0;

	return ret;
}


int get_gpio(enum control_pin pin)
{
	int ret = 0;

	ret = random() % 1;

	return ret;
}
